# gradio 6.x: the Chatbot "messages" format (role/content dicts) is the default,
# so the code does NOT pass type="messages". That argument was removed in gradio 6
# and now raises: Chatbot.__init__() got an unexpected keyword argument 'type'.
# (Developed and tested against gradio 6.17.3.)
gradio>=6.0.0
requests>=2.31.0

# Static mode is a semantic retrieval lookup (see static_dialogue.py): the
# player's question is embedded with a Sentence Transformer and matched against
# a fixed Q&A database stored in ChromaDB. These two pull in torch and are a
# large (~GB) first install, and the embedding model downloads once on first use.
chromadb>=0.5
sentence-transformers>=2.2
